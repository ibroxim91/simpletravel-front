version: "3.9"

services:
  simple-travel-frontend:
    image: username/simple-travel-frontend:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure
