'use client';

import AuthShell from '@/features/auth/ui/auth-shell';

const LoginClient = () => {
  return (
    <div className="h-full">
      <AuthShell />
    </div>
  );
};

export default LoginClient;
